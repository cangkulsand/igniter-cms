# /original-code — Frozen Baseline (BEFORE refactoring)

This folder is the **untouched original source** used as the "before" baseline for
UTM Part B (Code Smells & Refactoring). Do **not** edit anything here.

- **Contents:** a verbatim copy of the project's hand-written source tree (`app/`),
  taken before any refactoring.
- **Frozen at commit:** `4d49172` — *CodeIgniter 4 starter - Version 53*
- **Original author / source:** akassama — https://github.com/akassama/igniter-cms
- **Licence:** MIT
- **Working fork:** https://github.com/cangkulsand/igniter-cms

> Framework code (`vendor/`), front-end build assets (`public/`), and `node_modules/`
> are third-party/static and unchanged, so they are not duplicated here. All identified
> code smells and refactorings live under `app/`.

The refactored counterpart will be stored in `/refactored-code` after Phase 2.
